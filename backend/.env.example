# Cross-origin: frontend origin allowed by Nest CORS (see src/main.ts)
CORS_ORIGIN=http://localhost:3000

PORT=3001

# PostgreSQL (TypeORM)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=banshee
DB_USER=postgres
DB_PASSWORD=
